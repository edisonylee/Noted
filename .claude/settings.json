{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"$(git status --porcelain 2>/dev/null)\" && printf '%s' '{\"systemMessage\":\"📝 noted has uncommitted changes — consider committing before wrapping up (run git status to review).\"}' || true"
          }
        ]
      }
    ]
  }
}
